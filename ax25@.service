
[Unit]
Description=AX.25 %I Interface
After=network.target

[Service]
EnvironmentFile=/etc/ax25/interfaces.d/%i
Type=forking
ExecStart=/usr/sbin/axup ${DEV} ${AXPORT} ${HBAUD} ${IPADDR}
ExecStop=/usr/sbin/axdown ${AXPORT} ${MAINPID}

[Install]
WantedBy=default.target


